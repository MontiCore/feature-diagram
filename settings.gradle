/* (c) https://github.com/MontiCore/monticore */
pluginManagement {
    repositories {
        mavenLocal()
        maven {
            credentials.username mavenUser
            credentials.password mavenPassword
            url repo
        }
        gradlePluginPortal()
    }
}
rootProject.name = 'feature-diagram'
include ":fd-lang", ":fd-analysis"